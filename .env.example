# Used by .mcp.json to authenticate the Context7 MCP server.
# Export this in your shell (or your Claude Code environment config) —
# this file is a template only, never put the real key here or commit it.
CONTEXT7_API_KEY=
